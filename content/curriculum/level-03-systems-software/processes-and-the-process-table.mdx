---
title: Processes & the process table
level: level-03-systems-software
slug: processes-and-the-process-table
order: 1
timeMin: 10
learningOutcomes:
- Describe what a process table entry actually stores about a running program (PID, PPID, state, open files)
- Explain how fork() produces two nearly-identical processes and how their return values tell you which is which
- Predict what happens to a child's exit status if its parent never calls wait() on it
- Trace a process through its lifecycle states, including how a reaped exit differs from an unreaped one
knowledgeCheck:
- question: In the fork() example, what does the parent's copy of pid hold compared to the child's copy — and how does the code use that difference?
  whereToLearn: 'Covered in "Cloning yourself: fork()" — look at the if / else if / else branching on pid right after the fork() call.'
- question: What is a zombie process, and which line in the wait() example exists specifically to prevent one from lingering?
  whereToLearn: 'Covered in "Reaping children: wait() and zombies" — the waitpid() call is the line doing the reaping.'
assignment:
  instruction: Compile the fork() example above, then before running it, predict how many total lines of output a version that calls fork() twice in a row (back-to-back) would print. Run it to check yourself, then read man 2 fork for the exact rules on what's copied and what's shared.
  resourceName: man 2 fork
  resourceType: doc
  resourceUrl: 'https://man7.org/linux/man-pages/man2/fork.2.html'
additionalResources:
- title: Process states in Linux, explained
  type: video
  meta: 7 min
  url: 'https://www.youtube.com/watch?v=LGXtgexLNY4'
- title: man 2 wait
  type: doc
  meta: Documentation
  url: 'https://man7.org/linux/man-pages/man2/wait.2.html'
---

Every program you run — your editor, your browser, that C binary you just compiled — is a <b>process</b> to the operating system: a live, running instance with its own memory, its own open files, and one all-important number, its `PID`. The kernel doesn't run your code as a favor — it tracks every single process in a table and decides, thousands of times a second, whose turn it is to use the CPU.

This lesson is where "the shell" stops being magic. Type `ls` at a prompt and, behind the scenes, a brand-new row appears in that process table, runs for a few milliseconds, and vanishes. By the end you'll know exactly how that row gets created, what's inside it, and what happens if nobody cleans up after it.

## What lives in a process table entry

The kernel keeps a table with one row per running process — think of it as the OS's own spreadsheet of everything alive on the machine right now. Each row holds a <b>PID</b> (its ID), a <b>PPID</b> (its parent's ID), a <b>state</b> (running, sleeping, stopped, or zombie), and pointers to the process's memory and its table of open files.

You never edit this table directly — you only ever cause the kernel to add or update rows in it, by calling functions like `fork()`, `exit()`, and `wait()`. Every process except the very first has a parent, which is why the table naturally forms a tree with PID 1 (`init` or `systemd`) at the root.

<Visual type='diagram' caption='One row in the kernel&#39;s process table' description='A table with one row per running process and columns for: PID (unique process ID), PPID (the parent&#39;s PID), state (running / sleeping / stopped / zombie), a pointer to the process&#39;s memory (code, stack, heap), and a pointer to its open file descriptor table. Arrows connect a parent process&#39;s row to two child rows via matching PPID values, illustrating the process tree.' />

## Cloning yourself: fork()

`fork()` is the one syscall that returns <i>twice</i>. It clones the calling process — same code, same variables, same open files — into a brand-new process table entry, and then both the original (parent) and the clone (child) continue running from the exact same line, right after the call.

The only thing that tells them apart is the return value: the parent gets back the child's PID (a positive number), the child gets back `0`, and a failed fork returns a negative number to whoever called it (there's only one "whoever" then, since the child never got created). That single `if (pid == 0)` check is how every fork-based program branches into "parent work" and "child work."

<CodeBlock lang='c' linesJson='["#include &lt;stdio.h&gt;", "#include &lt;sys/types.h&gt;", "#include &lt;unistd.h&gt;", "", "int main(void) {", "    printf(\"before fork, pid=%d\\n\", getpid());", "", "    pid_t pid = fork();", "", "    if (pid &lt; 0) {", "        perror(\"fork\");", "        return 1;", "    } else if (pid == 0) {", "        printf(\"child:  pid=%d parent=%d\\n\", getpid(), getppid());", "    } else {", "        printf(\"parent: pid=%d child=%d\\n\", getpid(), pid);", "    }", "", "    return 0;", "}"]' runnable expectedOutput='before fork, pid=4021
parent: pid=4021 child=4022
child:  pid=4022 parent=4021' />

## Reaping children: wait() and zombies

When a child calls `exit()`, it doesn't vanish immediately — its process table entry sticks around as a <b>zombie</b>, just long enough to hold its exit status for the parent to collect. If the parent never asks, that zombie entry lingers until the parent itself exits or explicitly reaps it.

`waitpid()` is how a parent collects that status and frees the entry. This matters enormously for the shell you're about to build: every command you run is a child, and your shell must reap it or your terminal will slowly accumulate zombies every time you run a command.

<CodeBlock lang='c' linesJson='["#include &lt;stdio.h&gt;", "#include &lt;stdlib.h&gt;", "#include &lt;sys/types.h&gt;", "#include &lt;sys/wait.h&gt;", "#include &lt;unistd.h&gt;", "", "int main(void) {", "    pid_t pid = fork();", "", "    if (pid == 0) {", "        printf(\"child running, exiting with code 7\\n\");", "        exit(7);", "    }", "", "    int status;", "    waitpid(pid, &amp;status, 0);", "", "    if (WIFEXITED(status)) {", "        printf(\"child exited with status %d\\n\", WEXITSTATUS(status));", "    }", "", "    return 0;", "}"]' runnable expectedOutput='child running, exiting with code 7
child exited with status 7' />

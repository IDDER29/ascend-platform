---
title: 'Signals: how programs get interrupted'
level: level-03-systems-software
slug: signals
order: 4
timeMin: 11
learningOutcomes:
- Explain what a signal is and how it differs from a syscall (kernel-to-process vs. process-to-kernel)
- Install a signal handler with sigaction() and safely flag work for the main program to do outside of it
- Name which signals can never be caught, blocked, or ignored, and explain why
- Explain why a shell must catch SIGINT itself so Ctrl-C interrupts the running command, not the shell
knowledgeCheck:
- question: Why does handle_sigint() just set got_sigint = 1 instead of doing the real shutdown work directly inside the handler?
  whereToLearn: Covered in "Installing a handler" — the paragraph right after the code explains why handlers should do as little as possible.
- question: Why can't a process register a handler for SIGKILL the way it can for SIGINT?
  whereToLearn: Covered in "Some signals can't be caught" — it explains which signals bypass handlers entirely and why the kernel forces that.
assignment:
  instruction: Read man 7 signal for the full disposition table, then extend the sigaction example to also catch SIGTERM with its own handler that prints which signal number it received.
  resourceName: man 7 signal
  resourceType: doc
  resourceUrl: 'https://man7.org/linux/man-pages/man7/signal.7.html'
additionalResources:
- title: kill -l and signal numbers explained
  type: doc
  meta: Documentation
  url: 'https://linuxhint.com/kill-signal-numbers-linux/'
- title: 'Signals: the async event system of Unix'
  type: video
  meta: 9 min
---

Press Ctrl-C while a program runs and something interrupts it <i>immediately</i>, mid-instruction, with no cooperation required from the program itself. That's a <b>signal</b> — a tiny, asynchronous notification the kernel delivers to a process, completely independent of whatever that process happens to be doing at the time.

Signals are how the outside world reaches into a running process without a syscall being called <i>by</i> that process. Your shell needs this badly: when someone presses Ctrl-C, it has to interrupt the command it's running <i>without dying itself</i> — and that only works if you understand exactly how signals are delivered and caught.

## What a signal actually is

A signal is just a small integer (like `SIGINT` = 2, `SIGKILL` = 9, `SIGTERM` = 15) that the kernel delivers to a process — triggered by a keypress (Ctrl-C), another process calling `kill()`, or the kernel itself noticing something (like a segfault, which raises `SIGSEGV`).

Every signal has a <b>default disposition</b> — usually terminate, sometimes ignore, sometimes stop the process — but a process can override most of them by installing its own <b>handler</b>, a function that runs instead of the default when that signal arrives.

<Visual type='diagram' caption='A signal arriving mid-execution' description='A single process shown as a horizontal timeline of executing instructions. Partway along, a bolt icon labeled &#39;SIGINT delivered by the kernel (e.g. Ctrl-C at the terminal, or kill -SIGINT pid)&#39; interrupts the timeline. Below it, a branch shows execution jumping to the registered handler function, running it to completion, then resuming the original instruction stream exactly where it left off — illustrating that a signal handler runs asynchronously, squeezed in between normal instructions.' />

## Installing a handler

`sigaction()` registers a function to run when a given signal arrives, replacing that signal's default behavior. Because a handler can interrupt your program at <i>any</i> instruction, the rules for what's safe to do inside it are strict — the safest pattern is to do almost nothing: just set a flag and let your normal code react to it.

That's why the example below uses `volatile sig_atomic_t` instead of calling `printf()` directly inside the handler — printf isn't guaranteed safe to call from a signal handler, since it might interrupt an in-progress printf call from the main program.

<CodeBlock lang='c' linesJson='["#include &lt;stdio.h&gt;", "#include &lt;signal.h&gt;", "#include &lt;unistd.h&gt;", "", "volatile sig_atomic_t got_sigint = 0;", "", "void handle_sigint(int sig) {", "    (void)sig;", "    got_sigint = 1;", "}", "", "int main(void) {", "    struct sigaction sa = {0};", "    sa.sa_handler = handle_sigint;", "    sigemptyset(&amp;sa.sa_mask);", "    sigaction(SIGINT, &amp;sa, NULL);", "", "    printf(\"running - send SIGINT (Ctrl-C) to me\\n\");", "", "    while (!got_sigint) {", "        pause();", "    }", "", "    printf(\"caught SIGINT, shutting down cleanly\\n\");", "    return 0;", "}"]' runnable expectedOutput='running - send SIGINT (Ctrl-C) to me
caught SIGINT, shutting down cleanly' />

## Some signals can't be caught

`SIGKILL` (9) and `SIGSTOP` (19) can never be caught, blocked, or ignored — the kernel handles them itself, guaranteeing there's always a way to force-kill or force-pause any process, even a buggy one that ignores everything else. That's why `kill -9` always works.

For your shell project, the signal you'll actually care about most is `SIGINT`: your shell process needs its own handler so that Ctrl-C doesn't kill the shell itself, while the child process running the current command is left to receive SIGINT normally (or gets it forwarded) and stop. Get this wrong and every Ctrl-C during a long-running command kills your entire terminal session instead of just the command.
